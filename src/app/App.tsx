import React from 'react';
import { Layout } from "@ui";
import { AppRoutes } from "@routes";

const App: React.FC = () => {
    return (
                <Layout>
                    <AppRoutes />
                </Layout>
    );
  }


export default App;
